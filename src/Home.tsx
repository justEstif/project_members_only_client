const Home = () => {
  return (
    <div className="container my-10 mx-auto font-heading">
      Add form here
      <br />
      Shows messages under
      <br />
      Check against the database to get the messages and include token if it
      exists
    </div>
  );
};

export default Home;
